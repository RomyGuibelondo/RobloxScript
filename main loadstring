-- Automated Movement Script for Testing Purposes
-- This script moves the player in a square pattern

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

-- Function to move the character forward
local function moveForward()
    humanoid:Move(Vector3.new(0, 0, -1), true)
    wait(1) -- Wait for 1 second while moving forward
end

-- Function to turn the character right
local function turnRight()
    humanoid:Move(Vector3.new(1, 0, 0), true)
    wait(1) -- Wait for 1 second while turning right
end

-- Main loop to repeat movements
while true do
    moveForward()
    turnRight()
end
